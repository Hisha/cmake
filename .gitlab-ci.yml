# This file is a template, and might need editing before it works on your project.
# To contribute improvements to CI/CD templates, please follow the Development guide at:
# https://docs.gitlab.com/ee/development/cicd/templates.html
# This specific template is located at:
# https://gitlab.com/gitlab-org/gitlab/-/blob/master/lib/gitlab/ci/templates/Bash.gitlab-ci.yml

# See https://docs.gitlab.com/ee/ci/yaml/index.html for all available options

# you can delete this line if you're not using Docker
image: busybox:latest

stormLibRelease:
  stage: build
  script:
    - echo 'creating release'
  #  - cd $CI_BUILDS_DIR
  #  - mkdir stormLibReleaseX64
  #  - mkdir -p stormLibReleaseX64/lib
  #  - cp -R stormlib/includes stormLibReleaseX64/includes
  #  - cp -R stormlib/lib/
  release:
    name: 'StormLib Dependency $CI_COMMIT_TAG'
    description: 'Precompiled StormLib Libaray ready for use in windows platform applications.'
    tag_name: '$CI_COMMIT_TAG'
    assets:
      links:
        - name: 'StormLib'
          filepath: ''